package com.intranet.logquerytool;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the KQLBuilder Spring Boot application.
 */
@SpringBootApplication
public class KqlBuilderApplication {

    public static void main(String[] args) {
        SpringApplication.run(KqlBuilderApplication.class, args);
    }
}
